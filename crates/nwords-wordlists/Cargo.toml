[package]
name = "nwords-wordlists"
description = "Built-in wordlists for nwords."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[features]
default = ["std", "bip39-english", "adjective-animal", "named"]
std = ["nwords-core/std"]
adjective-animal = ["named"]
named = []
bip39-english = []
bip39-japanese = ["bip39-english"]

[dependencies]
nwords-core = { version = "0.1.0", path = "../nwords-core", default-features = false, features = ["alloc"] }
